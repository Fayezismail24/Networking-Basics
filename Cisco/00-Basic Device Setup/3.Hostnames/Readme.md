The hostname identifies your device. It appears in the CLI prompt and in logs.

```bash
Router(config)# hostname R1
R1(config)#
```

> **Best Practice:** Use descriptive names like `R1`, `SW-Floor1`, `Core-SW`.

---
