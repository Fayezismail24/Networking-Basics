When you first connect to a Cisco device (via console cable or terminal emulator), you enter **User EXEC Mode**.

```
Router>
```

> **Tip:** The `>` symbol means you are in User EXEC Mode (limited commands).  
> The `#` symbol means you are in Privileged EXEC Mode (full access).
