

1.WAN → DHCP Client (gets internet IP)

2.Create LAN Bridge

3.Add interfaces to Bridge (ether2, wlan1, wlan2)

4.Assign IP to Bridge (LAN gateway)

5.LAN → DHCP Server (gives IP to clients)

6.Optional: Enable DNS on LAN

Logic to remember: WAN asks → LAN gives
