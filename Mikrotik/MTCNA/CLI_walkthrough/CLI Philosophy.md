
MikroTik RouterOS uses a **menu-based CLI**.

- There are no configuration modes
- Commands are executed immediately
- Navigation is done through menus, not states

Think of the CLI as a **directory tree**, similar to a filesystem.

Example:
```bash
/ip address
/interface
/ip route
